const BenfitsImg = ({ studioImg }) => {
  return (
    <img
      src={studioImg}
      alt="fitness studio image"
      style={{ width: "100%", height: "100%" }}
    />
  );
};

export default BenfitsImg;
